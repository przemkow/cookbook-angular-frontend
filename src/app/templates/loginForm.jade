form(name='loginForm', ng-submit='loginVm.login(loginVm.credentials)', novalidate='')
  label(for='email') Username:
  input#email(type='text', ng-model='loginVm.credentials.email')
  label(for='password') Password:
  input#password(type='password', ng-model='loginVm.credentials.password')
  button(type='submit') Login
